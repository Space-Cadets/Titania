/**
 * Search Results for Classes / Teachers
 */
var React = require('react');
var Link  = require('react-router').Link;

module.exports = React.createClass({
  onClick: function() {
    //Do Cool Things -- (turn swag on)
  },

  render: function() {
    return (<div id="results-box">Results are here</div>);
  }
});
